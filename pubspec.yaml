name: app2030
description: A modern Flutter project for LOCAL app with Reels functionality.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 🧠 State Management & Navigation
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2

  # 🎨 UI & Design (سنستخدم Google Fonts بدلاً من الملفات المحلية)
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  shimmer: ^3.0.0
  lottie: ^3.1.2

  # 📱 Reels Features
  video_player: ^2.9.2
  chewie: ^1.8.5
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1

  # 🌐 Data & Utilities
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  intl: ^0.20.2
  path_provider: ^2.1.3
  share_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # تم تنظيف قسم الأصول ليعمل التطبيق حتى لو كانت المجلدات فارغة
  assets:
    - assets/images/
    - assets/icons/
    # - assets/videos/ # (قم بإلغاء التعليق فقط إذا أنشأت المجلد)

  # 🛑 ملاحظة: تم حذف قسم fonts الملحق يدوياً لأنه كان سبب "FAILURE: Build failed"